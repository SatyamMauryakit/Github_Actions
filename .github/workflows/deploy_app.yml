name: Deploy flask app
on:
  workflow_dispatch:
  jobs:
   
   deploy:
    environment:
    DOCKER_USERNAME: satyammaurya
    runs-on: self-hosted
    steps:
     - name: Checkout code
       uses: actions/checkout@v4
     - name: Deploy with docker compose
    run:  
          docker compose -f .github/workflows/docker_compose.yml up -d